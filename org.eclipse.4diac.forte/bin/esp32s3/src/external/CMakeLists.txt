SET(SOURCE_GROUP_BACKUP "${SOURCE_GROUP}")
SET(SOURCE_GROUP "${SOURCE_GROUP}/ESPIO")
ADD_SUBDIRECTORY("${FORTE_EXTERNAL_MODULES_DIRECTORY}/ESPIO" "${CMAKE_CURRENT_BINARY_DIR}/external/ESPIO")
SET(SOURCE_GROUP "${SOURCE_GROUP_BACKUP}")
SET(SOURCE_GROUP_BACKUP "${SOURCE_GROUP}")
SET(SOURCE_GROUP "${SOURCE_GROUP}/TESTE")
ADD_SUBDIRECTORY("${FORTE_EXTERNAL_MODULES_DIRECTORY}/TESTE" "${CMAKE_CURRENT_BINARY_DIR}/external/TESTE")
SET(SOURCE_GROUP "${SOURCE_GROUP_BACKUP}")
